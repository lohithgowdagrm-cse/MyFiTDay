services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_DB: myfitday
      POSTGRES_USER: myfitday
      POSTGRES_PASSWORD: myfitday_dev_only
    ports:
      - "5432:5432"
    volumes:
      - myfitday_pgdata:/var/lib/postgresql/data
volumes:
  myfitday_pgdata:
